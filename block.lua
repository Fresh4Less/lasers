Class = require "hump.class"
Vector = require "hump.vector"

Block = Class {
	init = function(self, pos) 
		self.pos = pos
	end
}
function Block:update(dt)

end
function Block:draw()

end

return Block